module.exports = {
  content: ["./src/**/*.{html,js,ts,jsx,tsx}"],
  darkMode: "media", // uses the browser's internal setting
  theme: {
    extend: {},
  },
  plugins: [
    require("daisyui")
  ],
}
